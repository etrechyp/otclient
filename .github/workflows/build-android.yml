name: Build Android APK Direct

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  build-android:
    uses: ./.github/workflows/reusable-build-android-apk.yml
